{
  "setupTasks": [
    {
      "command": "cd equipo-basket && npm ci",
      "name": "cd equipo-basket && npm ci"
    }
  ],

  "tasks": {
    "cd equipo-basket && npx ng build && npx http-server ./dist/equipo-basket/browser -p 8080 -s": {
      "name": "cd equipo-basket && npx ng build && npx http-server ./dist/equipo-basket/browser -p 8080 -s",
      "command": "cd equipo-basket && npx ng build && npx http-server ./dist/equipo-basket/browser -p 8080 -s",
      "runAtStart": true
    }
  }
}
