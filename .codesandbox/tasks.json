{
  // These tasks will run in order when initializing your CodeSandbox project.
  "setupTasks": [
    {
      "command": "cd equipo-basket && npm install",
      "name": "Instalar dependencias"
    }
  ],

  "tasks": {
    "start-angular": {
      "name": "Iniciar servidor de desarrollo",
      "command": "cd equipo-basket && npm start",
      "runAtStart": true
    },
    "cd equipo-basket && npx ng serve --host 0.0.0.0 --port 8080 --disable-host-check": {
      "name": "cd equipo-basket && npx ng serve --host 0.0.0.0 --port 8080 --disable-host-check",
      "command": "cd equipo-basket && npx ng serve --host 0.0.0.0 --port 8080 --disable-host-check",
      "runAtStart": true
    }
  }
}
